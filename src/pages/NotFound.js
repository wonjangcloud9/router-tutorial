const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
        fontSize: 64,
        position: "absolute",
        width: "100%",
      }}
    >
      <h1>Not Found</h1>
    </div>
  );
};

export default NotFound;
